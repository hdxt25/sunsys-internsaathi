apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: internsaathi-ingress
  namespace: internsaathi
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP":80},{"HTTPS":443}]'
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:us-east-2:839087537051:certificate/4790b6df-9708-40a3-bad5-5849c3a505d4
    alb.ingress.kubernetes.io/backend-protocol: HTTP
    alb.ingress.kubernetes.io/ssl-redirect: '443'
    alb.ingress.kubernetes.io/group.name: shared-alb
spec:
  ingressClassName: alb
  rules:
    - host: "www.hdxtdevops.win"
      http:
        paths:
          - path: "/"
            pathType: Prefix
            backend:
              service:
                name: internsaathi-frontend
                port:
                  number: 80
